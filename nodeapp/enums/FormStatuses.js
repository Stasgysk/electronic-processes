// FormStatuses.js

const FormStatusesEnum = Object.freeze({
    FILLED: 'filled',
    ACCEPTED: 'accepted',
    REJECTED: 'rejected',
    WAITING: 'waiting',
    INACTIVE: 'inactive',
});

module.exports = FormStatusesEnum;